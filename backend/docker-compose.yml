version: '3.9'
services:
  mysql:
    image: mysql:8.0
    container_name: healthmonitor-db
    environment:
      MYSQL_ROOT_PASSWORD: Kavi@2006
      MYSQL_DATABASE: health_monitor
    ports:
      - "3306:3306"
    volumes:
      - mysql_data:/var/lib/mysql
    healthcheck:
      test: ["CMD", "mysqladmin", "ping", "-h", "localhost"]
      interval: 10s
      timeout: 5s
      retries: 5

  app:
    build: .
    container_name: healthmonitor-app
    depends_on:
      mysql:
        condition: service_healthy
    ports:
      - "8081:8081"
    environment:
      SPRING_DATASOURCE_URL: jdbc:mysql://mysql:3306/health_monitor
      SPRING_DATASOURCE_USERNAME: root
      SPRING_DATASOURCE_PASSWORD: Kavi@2006
      SPRING_JPA_HIBERNATE_DDL_AUTO: update

volumes:
  mysql_data:
